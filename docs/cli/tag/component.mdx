---
title: "tag component"
description: "Create a new tag for an existing component artifact"
---

# cldctl tag component

Create a new tag for an existing component artifact and all its child artifacts.

<Note>
Use `cldctl tag comp` as shorthand for `cldctl tag component`.
</Note>

## Synopsis

```bash
cldctl tag component <source> <target> [options]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `<source>` | Source artifact reference |
| `<target>` | Target artifact reference |

## Options

| Option | Description |
|--------|-------------|
| `--artifact-tag <name=repo:tag>` | Override tag for a specific child artifact (repeatable) |
| `-y, --yes` | Non-interactive mode |

## Examples

```bash
# Tag an artifact
cldctl tag component ghcr.io/myorg/app:v1.0.0 ghcr.io/myorg/app:latest

# Using alias
cldctl tag comp ghcr.io/myorg/app:v1.0.0 ghcr.io/myorg/app:latest

# Non-interactive
cldctl tag component myapp:dev myapp:staging -y

# Override child artifact tag
cldctl tag component ghcr.io/myorg/app:v1.0.0 ghcr.io/myorg/app:latest \
  --artifact-tag deployment/api=ghcr.io/myorg/api:latest
```

## Output

```
$ cldctl tag component ghcr.io/myorg/app:v1.0.0 ghcr.io/myorg/app:latest

Tagging component artifact
  Source: ghcr.io/myorg/app:v1.0.0
  Target: ghcr.io/myorg/app:latest

Proceed with tagging? [Y/n]:
[success] Tagged ghcr.io/myorg/app:v1.0.0 as ghcr.io/myorg/app:latest
```

## See Also

- [`cldctl build component`](/cli/build/component) - Build a component
- [`cldctl push component`](/cli/push/component) - Push a component
