---
title: "validate environment"
description: "Validate an environment configuration"
---

# cldctl validate environment

Validate an environment configuration file without applying.

<Note>
Use `cldctl validate env` as shorthand for `cldctl validate environment`.
</Note>

## Synopsis

```bash
cldctl validate environment [path] [options]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `[path]` | Path to the environment directory or environment.yml file (default: current directory) |

## Options

| Option | Description |
|--------|-------------|
| `-f, --file <path>` | Path to environment.yml if not in default location |

## Examples

```bash
# Validate current directory
cldctl validate environment

# Using alias
cldctl validate env

# Validate specific file
cldctl validate environment ./envs/staging.yml

# Validate with custom file path
cldctl validate environment -f custom-environment.yml
```

## Output

```
$ cldctl validate environment ./envs/staging.yml

Environment configuration is valid!
```

**On validation error:**

```
$ cldctl validate environment ./invalid-env.yml

Error: validation failed: [VALIDATION_ERROR] component "web-app" missing required field "source"
```

## See Also

- [`cldctl create environment`](/cli/create/environment) - Create an environment
- [`cldctl update environment`](/cli/update/environment) - Update an environment
